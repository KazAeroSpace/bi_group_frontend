import type { FC } from 'react'
import styled from '@emotion/styled'

const Box = styled.div`
  display: flex;
  align-items: center;
  background-color: white;
  flex-direction: column;
  justify-content: space-between;
  height: 100%;
`

export const SlashScreen: FC = () => (
    <Box>

    </Box>
)
