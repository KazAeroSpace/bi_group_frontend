import { memo } from 'react'
import { Box } from './Box'

export const MapLayerList = memo(() => {
  return (
        <Box
            style={{
              width: 300,
              padding: 10
            }}
        >
        </Box>
  )
})
